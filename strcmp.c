#include<string.h>
#include<stdio.h>
main()
{
    char str1[6];
    scanf("%s",str1); //gets(str1);
    char str2[6];
    scanf("%s",str2); //gets(str2);
    strlen(str1);
    strlen(str2);
    strcmp(str1,str2);
    strcat(str1,str2);
    strcpy(str1,str2);
}
